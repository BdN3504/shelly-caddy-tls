# shelly-caddy-tls
It is hard to configure caddy to reverse proxy https to a shelly device. But we can use caddy to handle the certificate and when it's renewed, we can upload the certificate via rpc to the shelly device.
